---
name: aifabei-subsystem-builder
description: "为爱法贝零基础业务同事按业务模块新建、修改或接入独立系统，并与灼见中央 SaaS 保持兼容。一个模块可由多个部门共同参与，多个模块聚合成爱法贝企业。用户只要描述模块、参与部门、页面、操作和业务结果，本 Skill 自动补齐 iframe 上下文、统一操作接口、业务事件、健康检查和部署契约。用户提到爱法贝模块、跨部门协作系统、已有系统改造、ECS 上线或灼见接入时使用。"
---

# 爱法贝模块系统搭建

面向零基础同事，只提供三个入口：

1. **新建模块**：用户说“做一个样衣评审模块，设计部和生产部一起使用”并描述想看到什么。
2. **修改模块**：用户说“把生产协同模块的交期改成可编辑”。
3. **接入平台**：用户说“把这个模块接到灼见，完成后让设计部和采购部收到待办”。

不要要求用户理解微服务、HTTP、数据库、Docker、iframe 或消息队列。技术选择由本 Skill 承担。

## 业务层级

始终按以下关系理解需求：

```text
爱法贝企业
└─ 多个业务模块
   └─ 一个或多个参与部门（牵头、协作、审批或使用）
      └─ 页面、数据和操作
```

- **模块是主要交付单元**：围绕一个完整业务结果开发、部署、接入和迭代，不按部门拆成互相孤立的系统。
- **部门是协作角色**：一个模块使用 `departments` 列表声明全部参与部门及职责，不用单个 `department` 字段限制归属。
- **企业是聚合边界**：多个模块注册到同一个爱法贝企业空间，由灼见 SaaS 统一提供入口、身份、权限、AI 助手和跨模块协作。
- 默认一个独立系统承载一个模块；已有系统确有多个紧密相关模块时可以同仓库承载，但必须在清单中逐模块声明稳定标识、参与部门、页面和操作。
- 灼见 SaaS 不复制模块业务表，也不把模块实现硬编码进平台；它通过稳定清单、操作和事件协议动态接入，因此模块正常升级后无需每次人工重新对接。

## 先判断现状

1. 找到仓库根目录，按官方顺序完整读取全局与项目 `AGENTS.md`。
2. 运行 `python <本Skill目录>/scripts/inspect_subsystem.py --path <仓库根目录>`。
3. 读取 README、部署清单、Compose/Dockerfile 和现有健康检查。
4. 现有系统必须原地适配；不得套模板覆盖业务代码，不得重建用户数据库或移动文件。
5. 不确定业务含义时只问业务结果和协作关系，例如“哪个部门牵头、哪些部门协作，完成后谁收到待办？”不要问技术选型。

## 实施标准

按需读取 [平台接入最小协议](references/platform-contract.md)。所有接入系统至少具备：

- 可独立发布的网页地址；中央 SaaS 用 iframe 展示，因此子系统前端发布后平台自然看到新版。
- 页面上下文 `zhuojian:context` Bridge，让中央业务助手知道当前企业、模块、参与部门、实体和筛选条件。
- `/health` 健康检查。
- `/api/integration/manifest` 模块清单，逐模块声明参与部门、页面和可调用操作。
- `/api/integration/actions/{actionKey}` 统一操作入口；页面按钮和 AI 小助手必须复用同一个业务命令与权限校验，不能维护两套业务逻辑。
- 顺序变更出口 `/api/integration/events?after=...`，让中央 SaaS 或其他部门获知增删改。
- 独立服务 Token；Token、Cookie 和密码不进入 `postMessage`。
- 稳定的企业、模块、操作、实体标识和幂等 `eventId`。跨模块系统不得直接读彼此数据库，也不得复制整张业务表。

已有项目只补缺失部分。新项目选择满足业务的最小结构，不为“未来可能用到”预先堆服务。

## 模块内与跨模块协作

同一模块内的多个部门共享模块业务流程，但权限按部门职责细分。当模块 A 影响模块 B 时：A 完成业务写入并产生事件，中央集成层读取事件、按企业权限生成待办或调用 B 的受控操作。B 处理成功后记录 `eventId`，再推进游标。iframe 不是数据同步通道。

中央 SaaS 负责企业、用户、部门、模块注册、菜单、授权、操作目录、事件路由和聚合视图；模块系统负责自身业务数据、流程和操作规则。需要跨模块只读查看时，优先在中央平台做聚合视图或由 B 调用 A 的受控查询接口。

## 给下一个 AI 留路

完成仓库改造时：

- 在仓库创建简短的项目专属 Skill，写清业务地图、关键文件、验证命令和本项目事件类型。
- 在项目 `AGENTS.md` 中要求后续 AI 先读该 Skill。
- 提供可执行检查脚本，避免只留一篇长文。
- Skill 面向业务同事，默认直接行动，少用术语。

## ECS root 账号

用户可在需要时提供目标 ECS 地址、root 用户名和密码。按 [ECS 首次接入](references/ecs-first-access.md) 执行。

绝不把 root 密码写入仓库、Skill、`.env`、临时文件、命令参数、命令历史、日志或回复。密码只在当次交互的隐藏输入中使用。优先通过 GitHub + Coolify 发布；root 仅用于首次发现、绑定、排障或用户明确要求的直接部署。

## 完成标准

- 运行仓库自身测试、构建、Compose 配置和健康检查。
- 用真实浏览器验证目标业务流程、iframe、控制台和关键 2xx API。
- 用 `validate_endpoint.py` 验证 manifest 与事件游标。
- 告诉小白用户五件事：模块解决什么、哪些部门参与、页面与 AI 能做哪些相同操作、是否上线、下一步需不需要提供 ECS 登录信息。
