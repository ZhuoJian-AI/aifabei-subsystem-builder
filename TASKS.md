# Tasks

## SKILL-20260907-SERVER-ACCESS-MEMORY (@codex-root)

- Treat a novice's first `IP + root + password` submission as durable authorization for that server.
- Establish reusable local SSH-key access after the first verified password login and never ask the novice for credentials again.
- Publish stable Skill `v1.0.2` without changing subsystem contract revisions.
- Acceptance: unit tests, full Skill validation, public `1.0.1 -> 1.0.2` upgrade, and current-installation verification.
